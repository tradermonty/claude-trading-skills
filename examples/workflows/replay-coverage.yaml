schema_version: 1

covered:
  core-portfolio-weekly:
    spec: core-portfolio-weekly/replay.yaml
    variants:
      - required-only
      - full-path
  kanchi-dividend-weekly:
    spec: kanchi-dividend-weekly/replay.yaml
    variants:
      - required-only
      - full-path
  market-regime-daily:
    spec: market-regime-daily/replay.yaml
    variants:
      - required-only
      - full-path
  shapiro-contrarian:
    spec: shapiro-contrarian/replay.yaml
    variants:
      - required-only
      - full-path
  stockbee-20pct-study-daily:
    spec: stockbee-20pct-study-daily/replay.yaml
    variants:
      - required-only
      - full-path
  stockbee-ep-daily:
    spec: stockbee-ep-daily/replay.yaml
    variants:
      - required-only
      - full-path
  stockbee-fluency-loop:
    spec: stockbee-fluency-loop/replay.yaml
    variants:
      - required-only
      - full-path
  trade-memory-loop:
    spec: trade-memory-loop/replay.yaml
    variants:
      - required-only
      - full-path
  monthly-performance-review:
    spec: monthly-performance-review/replay.yaml
    variants:
      - required-only
      - full-path
  swing-opportunity-daily:
    spec: swing-opportunity-daily/replay.yaml
    variants:
      - required-only
      - full-path
  multi-asset-opportunity-daily:
    spec: multi-asset-opportunity-daily/replay.yaml
    variants:
      - required-only
      - full-path

# Replay coverage complete (11/11). The validator rejects adding a deferral, so a
# newly introduced workflow must ship both replay variants.
deferred: {}
